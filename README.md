The entire assignment is completed in a single Jupyter notebook. Run all cells will run all the tasks in order. 

Following packages are used in the assignment:
pip install numpy
pip install pandas
pip install scikit-surprise
pip install tensorflow-cpu
pip install shap
pip install lime
